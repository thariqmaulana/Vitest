import { describe, it, expect } from "vitest";

describe('sample', () => {
  it('should pass', () => {
    expect(1).toBe(1);
  });

  it('should pass 2', () => {
    expect(2).toBe(2);
  });

  it('should pass 3', () => {
    expect(3).toBe(3);
  });
});